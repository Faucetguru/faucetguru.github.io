# Blog Post Navigation Implementation Plan

> **For Hermes:** Use subagent-driven-development skill to implement this plan task-by-task.

**Goal:** Add navigation buttons to blog post HTML pages and integrate them with the main FaucetGuru index frame for seamless navigation.

**Architecture:** Add a shared navigation component to all blog post pages that links back to the main FaucetGuru site, the blog index, and provides prev/next post navigation. The nav will be injected via a shared JavaScript file loaded by each blog post page.

**Tech Stack:** Vanilla HTML, CSS, JavaScript (no framework)

---

### Task 1: Create shared blog navigation JavaScript

**Objective:** Create a reusable navigation component that can be loaded on any blog post page.

**Files:**
- Create: `blog/posts/blog-nav.js`
- Modify: All blog post HTML files in `blog/posts/*.html`

**Step 1: Write the navigation script**

Create `blog/posts/blog-nav.js` with the following code:

```javascript
(function() {
    const posts = [
        {
            title: "Ready Wallet (ex Argent X): la wallet inteligente de Starknet para Argentina",
            url: "ready-wallet-starknet-guia-argentina.html"
        },
        {
            title: "Tangem Wallet en Argentina: ¿la mejor billetera hardware para crypto?",
            url: "tangem-wallet-argentina-billetera-hardware-segura.html"
        },
        {
            title: "ACI Airdrop: cómo participar desde Argentina, guía paso a paso 2026",
            url: "aci-airdrop-guia-como-participar-desde-argentina.html"
        },
        {
            title: "Backpack Wallet review 2026: ¿Es la mejor wallet para Solana, Monad y Berachain?",
            url: "backpack-wallet-review-2026-es-la-mejor-wallet-para-solana-monad-y-berachain.html"
        },
        {
            title: "Reseña de ABC Mining: ¿Es confiable para ganar crypto en Argentina?",
            url: "abc-mining-reseña.html"
        }
    ];

    function getCurrentPostIndex() {
        const currentPath = window.location.pathname.split('/').pop();
        return posts.findIndex(post => post.url === currentPath);
    }

    function createNav() {
        const nav = document.createElement('nav');
        nav.className = 'blog-nav';
        nav.innerHTML = `
            <div class="blog-nav-inner">
                <a href="/" class="blog-nav-link">⚡ Faucet Guru</a>
                <a href="/blog/posts/index.html" class="blog-nav-link">Blog Index</a>
                <div class="blog-nav-spacer"></div>
                <a href="#" class="blog-nav-link" id="prev-post">← Anterior</a>
                <a href="#" class="blog-nav-link" id="next-post">Siguiente →</a>
            </div>
        `;
        return nav;
    }

    function setupPrevNext(nav) {
        const currentIndex = getCurrentPostIndex();
        const prevLink = nav.querySelector('#prev-post');
        const nextLink = nav.querySelector('#next-post');

        if (currentIndex <= 0) {
            prevLink.style.visibility = 'hidden';
        } else {
            prevLink.href = posts[currentIndex - 1].url;
        }

        if (currentIndex < 0 || currentIndex >= posts.length - 1) {
            nextLink.style.visibility = 'hidden';
        } else {
            nextLink.href = posts[currentIndex + 1].url;
        }
    }

    function injectNav() {
        const nav = createNav();
        document.body.insertBefore(nav, document.body.firstChild);
        setupPrevNext(nav);
    }

    if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', injectNav);
    } else {
        injectNav();
    }
})();
```

**Step 2: Create shared CSS for nav**

Add to `css/style.css` or create `blog/posts/blog-nav.css`:

```css
.blog-nav {
    background: #1a1a1a;
    border-bottom: 1px solid #333;
    padding: 12px 0;
    position: sticky;
    top: 0;
    z-index: 1000;
}

.blog-nav-inner {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 20px;
    display: flex;
    align-items: center;
    gap: 20px;
}

.blog-nav-link {
    color: #ff7357;
    text-decoration: none;
    font-weight: 600;
}

.blog-nav-link:hover {
    text-decoration: underline;
}

.blog-nav-spacer {
    flex: 1;
}
```

**Step 3: Verify file creation**

Run: `ls -la blog/posts/blog-nav.js blog/posts/blog-nav.css`

Expected: Both files exist

**Step 4: Commit**

```bash
git add blog/posts/blog-nav.js blog/posts/blog-nav.css
git commit --no-gpg-sign -m "feat: add shared blog navigation"
```

---

### Task 2: Add navigation to new blog post HTML files

**Objective:** Inject the shared navigation into the 5 new blog post HTML files.

**Files:**
- Modify: `blog/posts/ready-wallet-starknet-guia-argentina.html`
- Modify: `blog/posts/tangem-wallet-argentina-billetera-hardware-segura.html`
- Modify: `blog/posts/aci-airdrop-guia-como-participar-desde-argentina.html`
- Modify: `blog/posts/backpack-wallet-review-2026-es-la-mejor-wallet-para-solana-monad-y-berachain.html`
- Modify: `blog/posts/abc-mining-reseña.html`

**Step 1: Add script and CSS references to each HTML file**

For each file, modify the `<head>` section to include:

```html
<head>
  <title>[Post Title]</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <script src="/blog/posts/blog-nav.js"></script>
```

**Step 2: Verify one file**

Run: `head -10 blog/posts/ready-wallet-starknet-guia-argentina.html`

Expected: Shows the `<head>` with CSS link and script tag

**Step 3: Verify all files have nav**

Run:
```bash
for f in blog/posts/{ready-wallet-starknet-guia-argentina,tangem-wallet-argentina-billetera-hardware-segura,aci-airdrop-guia-como-participar-desde-argentina,backpack-wallet-review-2026-es-la-mejor-wallet-para-solana-monad-y-berachain,abc-mining-reseña}.html; do
  echo "--- $f"
  grep -q "blog-nav.js" "$f" && echo "✓ Has nav script" || echo "✗ Missing nav script"
done
```

Expected: All 5 files show "✓ Has nav script"

**Step 4: Commit**

```bash
git add blog/posts/*.html
git commit --no-gpg-sign -m "feat: add navigation to blog posts"
```

---

### Task 3: Integrate blog posts with main index frame

**Objective:** Update the main `index.html` to properly frame the blog section and provide navigation between the main site and blog posts.

**Files:**
- Modify: `index.html`
- Modify: `js/app.js`

**Step 1: Update `showBlog()` in `app.js`**

Modify the `showBlog()` function to add a "Back to Main Site" button:

```javascript
function showBlog() {
    hero.classList.add('hidden');
    faucetList.classList.add('hidden');
    faucetDetail.classList.remove('hidden');
    
    faucetDetail.innerHTML = `
        <button class="back-btn" id="back-to-main">← Volver a Faucets</button>
        <div id="blog-container" style="max-width: 900px; margin: 0 auto;">
            <p style="text-align: center; color: var(--text-dim);">Cargando blog...</p>
        </div>
    `;
    
    fetch('blog/posts/index.html')
        .then(response => {
            if (!response.ok) throw new Error(`HTTP ${response.status}`);
            return response.text();
        })
        .then(html => {
            const blogContainer = document.getElementById('blog-container');
            const parser = new DOMParser();
            const doc = parser.parseFromString(html, 'text/html');
            const bodyContent = doc.body.innerHTML;
            blogContainer.innerHTML = bodyContent;
            
            // Open external links in new tab
            blogContainer.querySelectorAll('a').forEach(link => {
                if (link.hostname && link.hostname !== window.location.hostname) {
                    link.setAttribute('target', '_blank');
                    link.setAttribute('rel', 'noopener noreferrer');
                }
            });
        })
        .catch(err => {
            const blogContainer = document.getElementById('blog-container');
            blogContainer.innerHTML = `<p style="color: var(--accent-orange);">Error cargando blog: ${escapeHtml(err.message)}</p>`;
        });
    
    document.getElementById('back-to-main').onclick = () => {
        faucetDetail.classList.add('hidden');
        hero.classList.remove('hidden');
        faucetList.classList.remove('hidden');
    };
}
```

**Step 2: Verify the function**

Run: `grep -n "function showBlog" -A 40 js/app.js`

Expected: Shows the updated function with back-to-main button

**Step 3: Commit**

```bash
git add js/app.js
git commit --no-gpg-sign -m "feat: add back button to blog view"
```

---

### Task 4: Update blog index links to use absolute paths

**Objective:** Ensure all blog index links use absolute paths for proper navigation.

**Files:**
- Modify: `blog/posts/index.html`

**Step 1: Update all links to absolute paths**

Replace all `href="filename.html"` with `href="/blog/posts/filename.html"`

Run:
```bash
python3 - <<'EOF'
from pathlib import Path
import re

index_path = Path('blog/posts/index.html')
content = index_path.read_text(encoding='utf-8')
content = re.sub(r'href="([^/"][^"]+\.html)"', r'href="/blog/posts/\1"', content)
index_path.write_text(content, encoding='utf-8')
print("Updated links to absolute paths")
EOF
```

**Step 2: Verify**

Run: `grep -n 'href="/blog/posts/' blog/posts/index.html | head -5`

Expected: Shows links with `/blog/posts/` prefix

**Step 3: Commit**

```bash
git add blog/posts/index.html
git commit --no-gpg-sign -m "docs: update blog index links to absolute paths"
```

---

### Task 5: Test and deploy

**Objective:** Verify the navigation works correctly and deploy to GitHub Pages.

**Files:**
- No file changes

**Step 1: Test locally**

Open `index.html` in a browser and verify:
- Click "Blog" button shows the blog index
- Click any blog post link opens the post
- Blog post has navigation bar with "Faucet Guru" and "Blog Index" links
- Prev/Next buttons work correctly
- "Back to Faucets" button returns to main site

**Step 2: Commit all changes**

```bash
git add .
git commit --no-gpg-sign -m "feat: add blog navigation"
```

**Step 3: Push to both branches**

```bash
git push origin main
git push origin gh-pages
```

**Step 4: Verify deployment**

Run:
```bash
curl -s https://faucetguru.github.io/blog/posts/ready-wallet-starknet-guia-argentina.html | grep -q "blog-nav" && echo "✓ Nav deployed" || echo "✗ Nav missing"
```

Expected: "✓ Nav deployed"

---

### Risks, Tradeoffs, and Open Questions

**Risks:**
1. The blog post HTML files are generated from `blog/markdown/*.md` - changes to HTML files may be overwritten
2. The `blog-nav.js` script needs to be loaded on each post page
3. The prev/next navigation depends on the posts array being in correct order

**Tradeoffs:**
1. Using a shared JS file vs. duplicating nav HTML in each post
2. Sticky nav vs. static nav at top of page

**Open Questions:**
1. Should the navigation be added to all blog posts or only the 5 new ones?
2. Should the nav include only the 5 new posts or all posts?
3. Should the blog posts be integrated into the main `index.html` frame or remain as separate pages?
